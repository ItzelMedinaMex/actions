name: main

on: [push]

jobs: 
    test-build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v2  #utilizamos checkout para situarnos dentro de nuestro repositorio
              
            - name: ls
              run: |  #"chmod +x"para darle permiso de ejecución 
                chmod +x script.sh
                ./script.sh   