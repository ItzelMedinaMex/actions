name: Test

on: [push]

jobs:
    test-build:
        runs-on: ubuntu-latest

        steps:
            - name: ls
              run: |
                    echo "hola mundito"  >> hola.txt
                    cat hola.txt


